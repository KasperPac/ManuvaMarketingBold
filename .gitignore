# build output
dist/
.astro/

# playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# generated: design-system files copied in for serving
public/_ds/

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# dependencies
node_modules/

# environment
.env
.env.production

# os / editor
.DS_Store
Thumbs.db

# photo originals: 6-9MB PNGs straight out of the generator. scripts/prep-photos.mjs
# turns each into the ~100KB JPEG that actually ships. Keep them locally for
# re-cropping; never commit them.
public/photos/*.png
