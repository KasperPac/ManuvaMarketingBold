Screen header. This is where the app is allowed the display face — 26px, one line, no wrapping.

```jsx
<TopBar domain="production" eyebrow="Production" title="Work orders" actions={<Button size="sm">New job</Button>} />
```
