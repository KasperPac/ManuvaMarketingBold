Underline tabs with optional counts. Active state is a 2px ink rule — never a filled pill.

```jsx
<Tabs value={tab} onChange={setTab} items={[{value:"open",label:"Open",count:12},{value:"done",label:"Completed"}]} />
```
