Lucide glyph, tinted with `currentColor`. Intentional addition: the source file ships no icon set.

```jsx
<Icon name="package" size={16} />
```

Domain glyphs: Inventory `package`, Products `layers`, Production `factory`, Purchasing `shopping-cart`, Orders `receipt`, Logistics `truck`, Audit `history`.
