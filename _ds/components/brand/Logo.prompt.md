The Manuva identity, masked to `currentColor` so it inverts for free.

```jsx
<Logo variant="lockup" height={26} base="../../" style={{color:"var(--ink-strong)"}} />
```

Clear space = the height of the mark's counter (0.25 × logo height) on all sides. Never recolour to a domain accent; ink or invert only.
