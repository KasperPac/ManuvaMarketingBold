The loudest tier of the expression hierarchy: ads, OG cards, deck covers, campaign heroes. Hard corners, one number, one line.

```jsx
<PosterBlock ratio="1200/630" fill="ink" eyebrow="Manuva · Inventory"
  numeral="1,240" unit="units short" headline="Know before the line stops."
  footer={<Logo variant="lockup" height={22} />} />
```

Never use inside the product UI. One PosterBlock per surface.
