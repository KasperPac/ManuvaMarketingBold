Status chip — the shipped `_ui/status-badge` primitive. Variant carries meaning and cannot be reassigned: success = done/in stock, warning = at risk/low, danger = blocked/short, info = in progress, default = draft/archived.

```jsx
<StatusBadge variant="danger" dot>Short 240</StatusBadge>
```
