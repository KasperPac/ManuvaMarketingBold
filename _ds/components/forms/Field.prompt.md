Label + hint/error wrapper. Labels are 12px semibold, sentence case, never colons.

```jsx
<Field label="Reorder point" hint="Triggers a purchase suggestion." required>
  <Input align="right" suffix="ea" />
</Field>
```
