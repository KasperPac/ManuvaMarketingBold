Square icon-only control for toolbars, table row actions, and topbar utilities.

```jsx
<IconButton label="Filter" variant="ghost"><Icon name="filter"/></IconButton>
```

`label` is required (a11y + tooltip). Use `active` for sticky toggle state.
