Radio input; group with a shared `name`. Optional `description` line for consequential choices.

```jsx
<Radio name="alloc" checked label="Allocate on release" description="Stock is reserved when the job starts." />
```
