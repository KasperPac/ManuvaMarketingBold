Empty states are the one place the app is allowed poster energy. Lead with the numeral (usually `0`), then a display-face line, then one action.

```jsx
<EmptyState numeral="0" title="No open production orders" body="Release a job to start allocating stock." action={<Button>New production order</Button>} />
```
