Persistent in-page message — sync failure, shortage warning, plan limit. Toast is transient; Banner stays until resolved.

```jsx
<Banner tone="warn" title="3 components short for job 4192" action={<Button size="sm" variant="secondary">Purchase</Button>} />
```
