Modal for confirmations and short forms. Title uses the display face at 20px.

```jsx
<Dialog eyebrow="Purchasing" title="Send PO-1042 to supplier?" onClose={fn}
  footer={<><Button variant="secondary">Cancel</Button><Button>Send</Button></>}>…</Dialog>
```
